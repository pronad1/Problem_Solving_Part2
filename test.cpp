#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    const int N = 1000000; // 1 million
    int arr[N];            // Static allocation (stack)
    cout << "Array of size " << N << " created on stack.\n";
    return 0;
}